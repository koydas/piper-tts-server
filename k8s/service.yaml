apiVersion: v1
kind: Service
metadata:
  name: piper
  annotations:
    # Next free MetalLB slot after .245 (whisper) — see gitops-homelab's
    # appproject/ADR-0016 for the full IP allocation list.
    metallb.io/loadBalancerIPs: "192.168.1.246"
spec:
  type: LoadBalancer
  selector:
    app: piper
  ports:
    - port: 8000
      targetPort: 8000
